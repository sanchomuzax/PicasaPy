# A QML-betöltés ma: 2558 ms, az indulás 76%-a (#1612)

*Mérés: 2026-09-02, RPi5 (a fejlesztői gép), üres könyvtár, `scripts/indulas_meres.py`.*

## 1. A jegy premisszája ELAVULT — rosszabb irányba

A #1612 **1914 ms**-ot idéz a #1601 méréséből. A mai szám:

| futás | QML betöltése (`Main.qml`) | teljes indulás |
|---|---:|---:|
| 1. (hideg) | **3500 ms** | 4893 ms |
| 2. (meleg) | **2558 ms** | 3367 ms |

⇒ A szakasz **nem javult, hanem nőtt** — és mostanra az indulás
**76,0%-a**. (A #1720 −41%-os nyeresége valós volt; azóta viszont a fa
újra hízott.)

## 2. A 2558 ms összetétele — MÉRVE, nem becsülve

### 2.1 Fordítás vs. példányosítás

Külön processzekben, csak a `QQmlComponent` felépítéséig mérve
(`QUrl` → `Status.Ready`, példányosítás NÉLKÜL):

| eset | fordítás |
|---|---:|
| hideg lemez-gyorstár (1. futás) | 536 ms |
| **meleg lemez-gyorstár (2–3. futás)** | **179 / 137 ms** |
| lemez-gyorstár KIKAPCSOLVA (2 futás) | 519 / 541 ms |

⇒ **A Qt lemez-gyorstára már működik, és ~4× gyorsít** a fordításon
(536 → ~140 ms). A `QML_DISABLE_DISK_CACHE=1` kontrollfutás bizonyítja,
hogy tényleg a gyorstár a különbség, nem a fájlrendszer melegedése.

⇒ **Az előfordítás (`qmlcachegen`) tehát NEM a megoldás**: azt a részt a
Qt már megoldja. A meleg indulás 2558 ms-ából a fordítás **~140 ms**;
a maradék **~2,4 s a fa PÉLDÁNYOSÍTÁSA**.

### 2.2 Mi épül fel — komponensenként

12 750 QObject. A #1720 módszerével (a fát bejárva, minden objektumot a
legfelső saját komponenshez könyvelve):

| befoglaló komponens | objektum | a fa %-a | kell az ELSŐ képkockához? |
|---|---:|---:|---|
| `PicasaMenuBar` | 3148 | 24,7% | **igen** |
| `PhotoViewer` (néző + szerkesztő) | 2868 | 22,5% | **nem** — csak kép megnyitásakor |
| `SplitView` (hasáb + rács + panelek) | 2466 | 19,3% | **igen** |
| `CollagePanel` | 1337 | 10,5% | **nem** — csak a Kollázs lapon |
| `PhotoContextMenu` | 586 | 4,6% | **nem** — csak jobbklikkre |
| `FileOpsDialogs` | 553 | 4,3% | **nem** |
| `TrayBar` | 448 | 3,5% | igen |
| `CreateDialogs` | 306 | 2,4% | **nem** |
| `ConfirmDialog` (5 példány) | 288 | 2,3% | **nem** |
| `PicasaImportDialog` · `NameInputDialog` · `InitialScanDialog` · `CollageDraftDialog` · `PerfMonitorPanel` | 272 | 2,1% | **nem** |

Típusonként a legnagyobb tételek: `CheckIndicator` (318), `MenuItem`
(224), `PicasaButton` (124), `MenuSeparator` (87), `PicasaMenuItem` (70).

## 3. Következtetés — hol a nyereség

**Halasztható, ma mégis felépül: ≈ 5942 objektum, a fa 46,6%-a.**

A gépezet MEGVAN (`DeferredDialog`, #1720) — 14 párbeszéd már így megy.
A maradék három csoport, növekvő kockázat szerint:

| lépés | nyereség | kockázat |
|---|---:|---|
| a) a maradék párbeszédek (`FileOpsDialogs`, `CreateDialogs`, `PicasaImportDialog`, `NameInputDialog`, `InitialScanDialog`, `CollageDraftDialog`, `PerfMonitorPanel`, 5× `ConfirmDialog`) | ~1419 obj (11,1%) | kicsi — ugyanaz a minta, csak hívóhelyeket kell `ensure()`-re írni |
| b) `PhotoContextMenu` | 586 obj (4,6%) | közepes — jobbklikkre nyílik, a megnyitás előtt kell felépülnie |
| c) `PhotoViewer` + `CollagePanel` | **4205 obj (33,0%)** | nagy — `id`-vel hivatkozott, sok kötés és teszt függ tőle |

⚠️ **Amit NEM szabad**: a halasztott részt helyfoglalóvá tenni. A
`DeferredDialog` épp ezért dob `TypeError`-t `ensure()` nélkül —
LÁTHATÓ hiba, nem néma hatástalanság (a projekt visszatérő kára).

## 4. Amit a mérés NEM dönt el

- **Windowson mennyi**: a #1653 mérőkocsi CI-futón méri; ez a lap a
  fejlesztői gép (RPi5) száma.
- **A c) lépés valódi nyeresége**: a 0,15 ms/objektum átváltás a #1720
  A/B-ablációjából származik; a `PhotoViewer` halasztása után ÚJRA kell
  mérni, nem szorozni.

## 5. Az a) lépés MEGMÉRVE — háromból kettő NEM halasztható

A benchmark a) lépése három párbeszédet ígért „kicsi kockázattal". A
kísérlet elvégezve; a kockázat NEM volt kicsi, és ezt két meglévő őr
mutatta meg, nem az élesben jelentkező hiba:

| párbeszéd | mit hozott | verdikt |
|---|---:|---|
| `NameInputDialog` | **−62 objektum** | halasztva ✅ |
| `CollageDraftDialog` | −3 (zaj) | **visszavéve** — mérve nulla |
| `CreateDialogs` | (−304 volna) | **visszavéve** — némán elveszne egy jelzés |

Végállapot: **12 743 → 12 681 objektum (−62, −0,5%).**

### 5.1 `CollageDraftDialog` — a halasztás mérve NULLA

Az `openIfNeeded()` a főablak `Component.onCompleted`-jében fut, **feltétel
nélkül**, tehát az `ensure()` ugyanabban a pillanatban felépítené. A/B:
halasztva 12 380, halasztás nélkül 12 377 — a különbség a zajon belül.

### 5.2 `CreateDialogs` — a #1743 őre fogta meg

A `CreateDialogs.qml` **két** `Connections { target: controller }` blokkot
tart; a fájl végi a kollázs/film EREDMÉNYÉT jelzi vissza
(`onCollageFinished`, `onCollageFailed`, `onMovieProgress`). A kollázs a
**Kollázs panelről** is indítható — halasztva a kezelő nem létezik, tehát a
panelről indított kollázs visszajelzése **némán elveszne**. Előfeltétel:
a hallgatók a `Main.qml`-be (#2096).

### 5.3 A tanulság a b)/c) lépéshez

**Halasztani csak azt érdemes, ami (1) FELTÉTELES úton épül, és (2) nem tart
a gazdájára mutató jelzés-hallgatót.** Az a) lépés 1419 objektumos ígéretéből
ezért maradt 62 — a b)/c) becsléseit ugyanígy MÉRNI kell, nem szorozni.
